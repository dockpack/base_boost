---
dependency:
  name: galaxy
  requirements_file: requirements.yml
driver:
  name: docker
verifier:
  name: goss
  lint:
    name: yamllint
platforms:
  - name: base-boost-centos7
    image: centos:7
    privileged: true
  - name: base-boost-centos6
    image: centos:6
    privileged: true
  - name: base-boost-ubuntu
    image: ubuntu:xenial
    privileged: true
provisioner:
  name: ansible
  config_options:
    defaults:
      remote_tmp: /tmp
      stdout_callback: yaml
  playbooks:
    prepare: prepare.yml
    converge: converge.yml
  lint:
    name: ansible-lint
scenario:
  name: default
